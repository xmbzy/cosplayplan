# CosPlan 1.5.10 更新说明 · 并外部实验稿的两件：外观（深色）+ 全局搜索；新手引导加两步

> 交付物：`cosplan-1.5.10.html`（704,565 字节 / 9,998 行 / CRLF / md5 `a7eab40b839ff5bfa9573c21537bd487`）—— **没出 APK**，手机上仍是 `release-kit/CosPlan-v1.5.9.5-release.apk`
> 基线：`cosplan-1.5.9.11.html`（md5 `398c9b334176d9f6bc1dc4e4561fb042`，冻结不动，本版一字未改）
> 输入：你给的两份外部实验文档 `D:\企鹅wb\cosplan\versions\cosplan-1.6.0-wb.1.html`（外观 / 深色）与 `cosplan-1.6.0-wb.2.html`（全局搜索）。**两份的底座就是本版基线**（那边留着 `cosplan-1.5.9.11.base.html`，md5 与基线逐字节一致），所以并进来没有跨线合并的坑。
> 你拍板的两件事：**并哪两件 = wb.1 + wb.2**（wb.3 履历文本统计 / wb.4 待结的钱 / wb.5 12 个月趋势图都没要）；**基线 = 本目录的 `cosplan-1.5.9.11`**（不跟 1.6 线走）。

---

## 0. 一句话与一张表

**功能被收进折叠之后，"看得见"这件事本身就要教 —— 这一版把两份实验稿里最能减少来回的东西并进来：一个框搜全部，和一套夜里不刺眼的深色。**

| 项 | 上一版（1.5.9.11） | 本版 | 说明 |
|---|---|---|---|
| 产物 | 682,620 B / 9,711 行 | **704,565 B / 9,998 行** | 43 条补丁；OLD 从冻结基线按行号切；两次 `--force` md5 相同 |
| 外观 | 只有一套浅色 | **跟随系统 / 浅色 / 深色三档** | 浅色取值逐像素不变（端到端 D1 实测 8 处 computed 与基线一字不差） |
| 搜索 | 各页各搜（装备页有搜索框、联系人页有搜索框…） | **顶栏一颗，一个框搜 12 类** | 项目 / 装备 / 角色 / 联系人 / 场地 / 拍摄 / 预约 / 待办 / 交付 / 花销 / 发布 / 意向 |
| 新手引导 | 20 步 | **22 步**（加「搜索」「外观」两步） | 加了新入口不更新引导 = 给新人留新盲区 |
| 产物自检 | 75 条 | **75 条 / 0 FAIL** | 其中三条被 token 化打红，改法是"或"关系（见 §4-5），两条线都跑得动、且比原来严 |
| 内联 JS 字符串白名单 | 131 处 | **131 处（新增 0 处，一个词都没放宽）** | 实验稿原本要为一个 `${w}` 撑开白名单，本版改写成 `data-q`（见 §2 末） |
| 独立门禁 `gate.js` | 144 项 | **156 项** | 新增 8m) 段 12 条（L1~L6 / S1~S6），负向 **12/12**；跑 1.5.9.11 仍 144、9.10 仍 131、9.9 仍 125、9.8 仍 112 |
| 本版端到端 | — | **`x1510_look.js` 17 条 + `x1510_search.js` 20 条，均 0 未过** | 深色对比度是**我自己量的**，没采信实验稿注释里的数字 |
| 全量 | 31 套 / 1102 条 | **33 套 / 1139 条 / 0 失败** | 31 份派生副本**逐套与 1.5.9.11 同数** = 零回归；1102 + 17 + 20 = 1139 |
| 数据模型 | — | **零变更** | 不加集合、`storeKey`/`schema` 不动；只多一个设置项 `meta.settings.theme`（与 `tourSeen` 同类：是偏好，跟着备份走） |

---

## 1. 外观 / 深色：并进来的是"做法"，不是"一套黑 CSS"

实验稿这套的架构是对的，我原样保留：**先把散在组件规则里的浅色硬编码收编成 token（浅色取值 = 旧值），深色只在文件末尾覆盖一套 token，组件规则一行不重写。** 所以：

- 翻面只有一层：`document.documentElement` 上的 `data-theme`，组件全部走 `var()`。门禁 L1 钉"只此一层"（第二处 `setAttribute('data-theme'` 或在深色块里重写组件规则都会红）。
- **浅色逐像素不变**不是嘴上说的：端到端 D1 拿同一屏（首页）在 1.5.10 浅色与基线 1.5.9.11 之间比 8 处 computed（顶栏 / 卡片 / 主按钮 / ghost / 筛选签 / 分页签 / 数字格 / 页面底），要求一字不差 —— 实测 0 差异。
- 深色下的对比度我自己量（不采信实验稿注释里那串数字）：首页 5 处 **7.15 ~ 18.69**，「我的」页三种提示框 / 徽章 / 选中签 / 折叠标题共 11 处 **6.32 ~ 14.1**，四档左竖条（非文字件）**5.17 / 7.15**（门槛 3:1），引导卡片 14.1。全部过线，门禁 L2 另外钉住三个新 token 的**浅色值本身**（收编时动过像素就红）。
- 档位语义：默认 `auto` 跟随系统（`matchMedia` 监听，系统切这边实时跟着翻，且只在 auto 下跟）；选浅色 / 深色就是钉住。**首绘前就定色**（`boot` 里 `applyTheme()` 在 `render()` 之前），深色用户不会先被闪一下白 —— L5 钉这条 + 监听幂等（`if(SYS_DARK_MQ) return;`）。
- **要发出去的纸面不跟着翻面**：装包单（`packDocText`）与确认文本（`termsDocHtml`）是两份自带 `<style>` 的独立 HTML，本版钉死它们不引用主题 token、不碰 `data-theme`（L6），端到端在**深色下**实际生成一遍验证（7,201 字符 / 1,198 字符，均不含 `var(--`）。打印与分享是白纸。
- 记在哪：`settings().theme`。这是**设置项**（跟备份走，实测 `buildExport()` 里有 `"theme":"dark"`），与自由行草稿那种临时物两类；`emptyData` / `migrate` 里不许出现它（L4）。
- 一条顺手记下的事实：`settings()` 会把缺失的 `theme` 归一成 `'auto'`，于是**任何一次保存**都会把它写进本机数据 —— 这与 `swipe`/`hint`/`topInset` 从 1.5 起就是同一口径，不是新增的空壳行。我本来按"读一屏不该落盘"写了条断言，跑出来红了，查清是断言写错了对象：真正要钉的是**只逛不保存时 localStorage 一个字都不变**（端到端 A2 改成这条，实测通过）。

## 2. 全局搜索：一颗入口、十二类、一段不筛

- 顶栏那颗「搜索」在**五个屏**都给了（首页 / 项目 / 装备 / 通讯录 / 我的）—— 搜索是"去哪都行"的动作，藏在某一页里就没人知道它存在。它**不占底栏格子**：`TABS` 仍 5 格、`TAB_OF.search='home'`（门禁 S5，负向里"给底栏加一格"必须红）。
- **一段记录都不筛**（红线在搜索上的落点）：已归档的一套、报废/已售的一件、已取消的一张，全都搜得到 —— 门禁 S1 查 12 类来源一个不少 + 函数体里没有藏档的筛子；端到端 C1/C2 各造一条真实"看不见的档位"，从函数与界面两层各验一次。
- 命中哪段高亮哪段，点一行**走既有跳转**（`go('project'…)` / `gotoTarget` / `formIntent`…，id 全过 `jsid`）。端到端 F1/F2/F3 真鼠标点三行，分别落到那一套 / 那一件 / 那位联系人。
- **每类最多 20 条，且当场说实话**：头部报「命中 25 条」，下面画 20 行并明写「还有 5 条没显示，换个更精确的词」—— 不是悄悄少给（S4 + 端到端 E1）。
- 空态不装死：没匹配时把**搜索词原样转义**回显并说"换个更短的词试试 —— 比如搜「芙」而不是全名"（I1）。
- **一处对实验稿的改写**：快捷词那排原本把词拼进内联 JS（`onclick="S.searchQ='${w}';render()"`），这道门禁白名单不为此放宽一个字母（`w` 这种单字母一旦进去，下次真出事的也是它）。改成 `data-q="${esc(w)}" onclick="gsQuick(this)"`，行为一字不差；S3 的负向 mutation 就是"把它改回拼字符串"，必须红。
- XSS 探测（D1/D2）：项目名塞 `<img src=x onerror=window.__pwned=1>'"><svg onload=…>`，搜它 —— 结果按字面显示、`#main` 里没被建出 `<img>`、`__pwned` 没被置位、命中段照样 `<mark>` 高亮。搜索词回显也走 `esc`。
- 只搜不写：全程 `DB` 与 `localStorage` 逐字不变（J1）。

## 3. 并法（怎么保证"并的就是他们写的，不多不少"）

这版不是一行行手抄，而是**把两份实验稿与基线逐行对齐后机械发射补丁**：

1. `qa/_wb_align.js` / `qa/_wb_emit.js`：贪心对齐（不匹配时往后找第一个能重新对上的行，找不到就按单行替换同进），wb.1 出 40 块、wb.2 再出 15 块，合并相邻后 40 块 + 我自己 3 块 = **43 条补丁**。
2. 纯插入的块原本 OLD 为 0 行（apply 要求 OLD 非空且全文件唯一）→ 发射器**自动撑窗**：向两侧扩到锚点唯一（最多 8 行），扩进来的未改动行原样补回 NEW 头尾；再合并相邻块，最后逐块复验"锚点命中恰好 1 次"。
3. `qa/_mk1510_patches.js` 的 marker 断言从历版的"**首行**必须含"改成"**OLD 块内**必须含"—— 本版有 19 个纯插入块，取锚后首行常是空行或 `}`，只查首行等于没查。
4. **字节级对账**（`qa/_1510_verify.js`）：产物 vs `wb.2` = 对方有我没有 **4 行**（版本印记 2 + 快捷词那颗 + 导出行），我有对方没有 **23 行**（版本印记、沿革 9 行、引导两步 6 行、`gsQuick` 改写 3 行、其余 3 行）—— 逐行看过，**没有夹带**。代码注释里的 `v1.6.0-wb.1 / wb.2` 标记**原样保留**：那是出处，不是笔误。
5. **EXPECT 3 条**（派生副本断言改写，咬空即构建失败）：① 逐屏文案白名单加词 `|新手引导|外观|跟随系统|搜索`（只加词，"少掉词"方向照旧红）；② ③ 引导套件的 `KEYS` 表按本版 22 步重写（**是加长不是放松**：步数、顺序、空库跳过的判据一条没减）+ 副本标题改版本中立。
6. **派生副本的 `#notour` 让路规则留了一条例外**（本版踩的坑，见 §4-1）：`x1510_tour.js` 留真 URL —— 它 A 段验的就是"首次打开真的自动播"，给自己挂上 `#notour` 等于把被测功能关掉。

## 4. 我自己这一轮抓到并改掉的错（照例逐条列）

1. **全量第一轮 8 条红，红在我自己的测试规则上**：给 31 份派生副本统一挂 `#notour` 时，把新手引导那一套也挂了 → 它 A 段"自动播"永远不可能成立（`A1 on=false`、`A7 steps=0`、`A9` 返回栈反而被那次真点击撑长）。**产品一字未改**（两轮 md5 相同），改成按目标文件名留例外。教训：**为测试全局让路的开关，必须检查"有没有把被测功能本身关掉"**。
2. `apply1510` 的派生换号链终点锚我又抄成"带分号"的那一行（上一版同款错）：`derive()` 里那行 `.replace(/run15910_full/g, …)` 末尾**没有分号**（分号在下一行 `#notour` 那条上），带分号当终点就一路吃到 `makeRunner`，把派生清单整段吞掉。
3. 顺着 #2 修完又踩一次：我的新链文本自己写了 `.split(…#notour')`，而区间终点之后的**原文件那行 split 还在**，两条 split 串在一起 → `Unexpected token '.'`。同一处逻辑现在改成了"终点停在 split 之前，让原行留下来"，并把它换成按文件名判断的例外式。
4. 三条历版自检被 token 化打红（`③ 四档左竖条`、`首页「需要处理」三档色条`、`④ TAB_OF`）。**没有一条是产品坏了**：判据把旧字面量写死了。改法是"或"关系 —— 旧字面量 **或** token 间接层 + 浅色那个值仍在（后者其实更严：它钉住了取值本身）。
5. **我把上一版刚立的规矩当场又犯了一遍**：为了让 `④ TAB_OF` 过，我第一版改成"必须同时有 `timeline:'home'` 和 `search:'home'`" → 1.5.9.9 / 9.10 / 9.11 三份旧产物立刻各红一条（它们没有搜索）。这就是 §21.12-A1 说的"拿版本号 / 新增物当所有版本都有的东西"。**判据要认功能**：`!/function vSearch\(/.test(src) || /search:'home'/.test(src)`。修完旧版复验 144 / 131 / 125 / 112 全绿。
6. 端到端 B1 我拿**猜的样本名**去搜十二类（'漫展服'、'滨江'…），装备类直接 0 命中 —— 不是搜索坏，是我猜的名字不在样本里。改成**从各类真实记录身上取连续三字**再断言命中；顺带第一版还犯了"把 `·` 去掉再取词"的错（标题在 hay 里是连续一段，拆着取必然搜不到）。
7. 端到端 E1 我伪造了一个 `pl` 传给 `packDocText`，它掉进 catch 返回 56 个字符的 `ERR …` —— 装包单的计划只能由 `packPlanFor(拍摄单号)` 现算。改成走真路径后拿到 7,201 字符 / 7 个分组。
8. 端到端两处"点不动"：`loadSample()` 之后 boot 那颗 700ms 的档期冲突弹窗会自己起来，把真点击挡住（`muteConflict('…') intercepts pointer events`）。产品口径正确（弹窗优先），是测试要先收窗 —— 加了 `dismiss()` 在每次真点击前等过 700ms 再收。
9. 负向验证两条 mutation 一开始咬不住：S5 的 needle 抄多了两个前导空格（命中 0 次，脚本直接点名）；S6 我改的是 `S` 对象里的 `ff:null`，而判据查的是 `emptyData` → **全绿（判据咬不住）**。各改一处后 12/12 红在该红的那条。
10. 引导文案自查：新加的「外观」一步原写"深色不刺眼，导出的图也跟着翻"—— 与实际相反（导出件刻意保持白纸）。改成明写"导出的海报与装包单仍是白纸底"。**界面/引导里的每句"会怎样"都要能在代码或端到端里被指出来。**

## 5. 有意没做的

- **wb.3 / wb.4 / wb.5 都没并**（你只点了前两份；wb.3 履历文本统计与 wb.4 待结的钱和主线 1.6.0 / 1.6.1 是同一件事的两套做法，并进来就要在三套里做取舍，那是另一个决定）。
- 深色**不做逐组件重新设计**，也不做主题编辑器 / 自定义配色：只覆盖一套 token，浅色是唯一基准。加色板 = 加一份要长期维护的对比度账。
- 搜索**不做拼音、模糊、纠错、权重排序**：词就是子串命中，按类型分组、每类 20 条。上排序就要解释"为什么这条在前面"，那是另一版的事。
- 搜索**不进时间轴与条款正文**（时间轴是现算视图、条款有留档快照，两处都不是记录列表）；要搜条款内容目前仍去那张单里看。
- **不出 APK**；`theme` 与 `tourSeen` 都跟着备份走，恢复后不会重新自动播、也不会把深色换回浅色。
- **1.6 线（履历墙 / 待结的钱）不含本版这两件**，两条线仍是并行产物。

## 6. 交付清单

| 文件 | 是什么 |
|---|---|
| `cosplan-1.5.10.html` | ★ 交付物（704,565 B / 9,998 行 / md5 `a7eab40b839ff5bfa9573c21537bd487`） |
| `qa/_wb_probe.js` · `_wb_align.js` · `_wb_read.js` · `_wb_show.js` · `_wb_emit.js` · `_1510_ranges.js` | 外部实验的只读探针 / 对齐 / 带上下文读 / 按名字读 / 发射补丁原料 |
| `qa/_1510_new.txt` · `qa/_1510_patches.txt` · `qa/_mk1510_patches.js` | NEW 块 43 · OLD/NEW 配对 · 从冻结基线按行号切 OLD（块内 marker 断言 + 区间互斥 + 撑窗取锚） |
| `qa/_mk1510_apply.js` · `qa/apply1510.js` | fork 脚本 · 构建 + 75 条自检 + 派生 31 份副本 + 生成 runner |
| `qa/gate.js` | 156 项（新增 8m) 段 L1~L6 / S1~S6；引导段适用范围改成**认功能**） |
| `qa/_gate1510_neg.js` | 12 条变异，逐条红在该红的那一条（12/12） |
| `qa/x1510_look.js` · `qa/x1510_search.js` | 本版两套端到端：17 条 / 20 条，均 0 未过 |
| `qa/run1510_full.js` · `qa/_r1510_full.log` | 33 套 / 1139 条 / 0 失败；31 份派生副本逐套与 1.5.9.11 同数 |
| `qa/_1510_verify.js` | 字节级对账：产物 = wb.2 + 我声明的 23 行（4 行被替换） |
| `qa-screenshots/T1510-*.png` | 深色-我的 / 深色-引导 / 深色-搜索 / 搜索-XSS探测（逐张看过） |
| `更新说明-1.5.10.md` | 本文件 |
