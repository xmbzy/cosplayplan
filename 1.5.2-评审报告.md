# CosPlan v1.5.2 评审报告

> 主理人：齐活林（Qi）｜评审日期：2026-09-13｜基线：1.5.1
> 方法：源码级走查（含完整 diff 逐行核对）+ **独立**复跑全量自测 + 真 Chromium 验证。

---

## 0. 指纹 & 改动规模

| 项 | 值 |
|---|---|
| 文件 | `cosplan-1.5.2.html` |
| 体积 | 342,744 字节（1.5.1=338,900 → +3,844） |
| 行数 | 5,179 行（全 **CRLF**，ratio 1.000，无裸 LF） |
| md5 | `569c4e3d1e4cc11dc7b1870b35e06383` |
| 标题 / APP.ver | `v1.5.2` / `ver:'1.5.2'` |
| 存储键 | `cosplan.data.v1` 未变，零新表零新字段 |
| 相对 1.5.1 diff | **92 行**（新增 75 / 删除 17）—— 小批次 |

---

## 1. 五项改动 —— 源码走查结论（全部属实）

### ① 时装名默认值修正 ✅（但见 §3 P2）
- `derive` 重写（L1752-1756）：`if(skinTouched) return;` → `__new` 且空则填"原皮" → 否则 `skin==='原皮'` 则清空。
- 语义：新建角色默认原皮、已有角色保持空白、`__new` 切回已有清掉自动填的原皮、用户手填过（`onTouch` 置 touched）一概不动。与更新说明一致。

### ② 重拍检测 ✅
- `cnNum(n)`（L1723-1733）：中文数字正确 —— 2→二、10→十、11→十一、21→二十一、过百回退阿拉伯数字。边界处理对。
- `formProject.onSave`（L1780-1804）：仅 `!id`（新建）且有 skin 时，按 `characterId+skin` 过滤现有项目、按 `updated_at` 倒序取最近一套为 `prevSame`；无自定义名且存在同名 → `autoName+'（第'+cnNum(same.length+1)+'次拍摄）'`。
- **装备继承**：把 `prevSame` 的装备逐个 `projectIds.concat([新id])` 并重跑 `syncItemExpense`。
- **钱不重复计**：核对 `syncItemExpense`（L1508-1526）确为「非空即锁定」——流水按 `aKey('item',id,'buy')` 一件一条，已归属项目就锁定复用，继承不会新增流水。更新说明属实。**人员不继承**：无任何 session/appointment 拷贝代码，符合预期。

### ③ 出装日选套标题 = 时装名 ✅
- `onsitePickProj` 行（L4719-4720）：标题 `p.skin||p.name||'未命名出装'`，副行 `阶段·时间·装备就绪 · 项目全名`。多套同角色可辨。

### ④ 自加项标「点击折叠」✅
- 现场页折叠组摘要（L4900）：`应急包 / 自加项　N 项 · 点击折叠`。

### ⑤ 清单滑动手势 ✅
- 拍摄单 `check-row`（L3433-3438）与现场页 `os-row`（L4825-4830）均加 `class="sw" data-sw="check:${sid}|${cid}"`，内容用 `swWrap` 包裹（`swWrap` 定义在 L2454，与既有 project/todo 滑动共用）。
- 引擎新增 `check` 类型（L4100-4114）：`l`（右滑）= 勾上/再右滑取消，`r`（左滑）= 移除；**装备行 `r:null`**（`isItem` → 不可移除，避免"刚删又被未同步加回"死循环）。
- 移除逻辑抽成 `removeCheckRow`（L3487-3498），✕ 按钮 `delCheck` 复用它 + render；应急包行照旧记入 `kitOff`。
- **执行链正确**：滑动动作经 `applyAction(d.action.msg, d.action.run)`（L4225），而 `applyAction`（L4044-4052）= 快照 → `mut(); save(); render();` → `toastUndo`（4 秒撤销）。勾选与移除**都可渲染、可撤销**。

---

## 2. 独立复跑全量自测（非采信自检）

```
OK   94/0  _sandbox_test_152.js        (1.3 基线)
OK   77/0  qa/z152_k13_logic.js
OK   96/0  qa/z152_regress_96.js
OK   61/0  qa/z152_audit.js            (一致性 + 口径扫描)
OK    2/0  qa/g152_deadbtn.js          (死按钮扫描)
OK   19/0  qa/g152_features.js         (沙箱守卫)
OK   13/0  qa/g152_browser.js          (真 Chromium)
==== 总计 PASS 362 / FAIL 0 ====
```

与更新说明"362/0"一致，由我本人复跑得出。真 Chromium 13/0 覆盖三种时装名路径、重拍保存（名字带第二次拍摄 + 装备继承）、选套标题、清单 data-sw 全覆盖与步骤函数真实执行、折叠组文案，**0 JS 报错**。

---

## 3. 分级

**P0 / P1：无。**

### 🟠 P2-1（本版新引入的体验回归，测试未覆盖）
**编辑任何「时装名 = 原皮」的已有项目时，时装名会被清空，保存被必填校验拦下、强制重填。**

- 证据链：
  - `applyDerives()` 在表单**每次渲染前都跑**（L1268），skin 字段无 `show` 条件 → derive 必然执行；
  - `derive` 对 `characterId!=='__new' && V.skin==='原皮'` 执行 `V.skin=''`（L1754-1755）；
  - 对**已存在项目**（`formProject('p1')`，`skinTouched` 初始 false、cur.skin='原皮'）→ 打开即被清空；
  - 保存时 `checkReq`（L1331-1345）见 `V.skin===''` 且 `req:1` → `toast('请填写：时装名')` 并 `return`。
- 影响面：**1.5.1/1.5.2 用「新建角色」建的项目，skin 会存成"原皮"** → 这类项目每次编辑都要重填时装名。
- 为什么算回归：1.5.1 不会清；且清空后**项目名里仍留着"·原皮"**（name 非空不重算）→ 名与字段不一致。
- 非数据丢失：req 校验会拦下，不会静默存空。但属明显体验退化。
- 建议修法：derive 只在"联动源刚变化"时清（记录上一次 characterId，或对已有项目用一个"初始值"标记跳过首次 derive）；或把清空条件收紧为"本次会话内由用户从 __new 切到已有"。**建议补一条测试**：`fSkin.derive({characterId:'c1', skin:'原皮'})` 应保持 `'原皮'`。

### 🟡 P3（打磨）
- **P3-1 重拍序号会"跳号/重号"**：`cnNum(same.length+1)` 依现有同套装项目数 +1。若删掉中间一套（如同角色同时装曾有 2 套、删 1 套），再建会算成"第二次拍摄"，与已存在的命名可能重复或跳号。建议按"历史最大序号 +1"而非"当前条数 +1"。
- **P3-2 继承装备的花费留在原项目**：因「非空即锁定」，继承到新项目的装备其流水仍归原项目 → 新项目的「预算与实际」不含这些装备支出，但装备却出现在新项目清单里。属既有设计使然，但可能反直觉，值得在 UI 上加一句说明。

### 🔴 债务追踪（连续第三版未清）
| 编号 | 内容 | 1.5.2 状态 |
|---|---|---|
| 1.5.0 P3-1 | 滑动推进 `done` 路径缺 `archived_at` 首转守卫（L4121 原样）→ 退回再归档会重弹复盘报告 | ❌ 未修 |
| 1.5.0 P3-2 | 复盘"利用率=勾选过"注释与实现张力（L3837-3838 逐字未动） | ❌ 未修 |
| 1.5.0 P3-3 | 预算"忽略告急后仍会提醒超支"措辞（L3721 仍只有"忽略这条"） | ❌ 未修 |
| 1.4.x | 图片库与 JSON 备份分离 / `imgDel` 在 `!IMG.ready` 漏清孤儿 blob（L787 原样）/ `projects.need` 冗余 `restore` | ❌ 未修 |

> 说明：1.5.1、1.5.2 都是 UX/功能批次，未碰债务。**"还债小版本"这一档至今是空的**——这是我从 1.5.0 起第三次提出，建议尽快安排。

---

## 4. 仍需你真机确认（更新说明已列，复述）

1. 清单滑动手感（`SW_MIN=46px` 力度阈值）合不合手。
2. 滑动与整行点击是否偶发误触（引擎理论上吞掉滑动后那次 click）。
3. 重拍按"角色+时装"匹配——若你习惯同时装跨作品复用（联动皮肤），匹配键可调。
4. **另加**：上面 P2-1 的复现（编辑一个时装名为"原皮"的项目，看是否被要求重填）。

---

**结论：v1.5.2 五项改动全部属实、实现干净，独立复跑 362/0 全绿，无 P0/P1，可日常使用。但发现一条本版新引入、测试未覆盖的 P2（编辑"原皮"项目会清空时装名并强制重填），建议下一版优先修掉并补测试；同时 1.5.0 起的三条 P3 与 1.4.x 三项老账**已连续三版未清**，强烈建议下一个版本专门还债。**
