# CosPlan v1.3 更新说明

文件：`cosplan-1.3.html`（1.2.2.2 / 1.2.2 / 1.2 / 1.1.2 / 1.1 / 1.0 全部保留不动，日常用这一版）
时间：2026-09-10
一句话：**常用的改动手边就能做（双击、长按、出装日），手机返回键不再把应用关掉，电脑端也能滑滑动槽。**

---

## 1. 装备详情页：双击一行直接改

以前只想改个「放在哪儿」，也得点「编辑」→ 在整张表单里找到那一格 → 保存。现在：

- **双击台账里任意一行**，弹出一张小卡片，只有这一项（或最相关的两三项），带**确认 / 取消**两个按钮。取消就什么都不动。
- 手机上「双击」＝**快速连点两下同一行**（原生 dblclick 在触屏上不稳，自己判的 420ms 连点）。
- 标题旁的**装备名**、上面的**状态徽章**也能双击改。

| 双击哪一行 | 卡片里出现什么 |
|---|---|
| 装备名 / 标题 | 叫什么（留空会按「角色 · 分类」自动起名，重名自动加序号） |
| 状态徽章 | 现在到哪一步 +（如果是处置态）最后怎么了、去向 CN、日期、售价、原因 |
| 所属角色 | 哪个角色（或通用） |
| 关联项目 | 用在哪个出装（多选） |
| 尺码 / 数量 | 尺码（消耗品还会带库存与提醒阈值） |
| 价格 | 花了多少钱（改完自动流水同步） |
| 渠道 / 链接 | 来源、店铺、商品链接 |
| 下单 / 到手 | 两个日期 |
| 存放位置 | 放在哪儿 |
| 制作进度 / 易损 | 自制阶段、进度 %、运输注意 |
| 毛色 / 造型 | 毛色、长度、造型状态、造型归属 |
| 品牌 / 度数 / 开封到期 | 美瞳那一组 |
| 去向 / 售价 / 原因 | 处置一组（与状态卡片同一份字段） |
| 试装记录 / 参考链接 / 备注 | 对应那一组 |

- 卡片里的**处置字段与「编辑」表单是同一份定义**（`itemDisposalFields()` / `itemDisposalRules()`），不会出现"这边要填去向、那边不要"的两套口径。
- 改完的提示条带**「撤销」**，4 秒内可以整体回退（含被带动的自动流水）。

---

## 2. 手机返回键：先回上一屏，真要退出才问

**之前**：在页面里按手机返回键 = 直接关掉整个 CosPlan，得重新打开。
**现在**：每一屏都记进返回栈，按返回键**回到上一个页面**；只有已经站在最上层（首页）时，才弹一个确认浮窗。

| 你在哪 | 按返回键 |
|---|---|
| 装备详情 / 项目 / 拍摄单 / 联系人… | 回到上一屏（顶部「‹ 返回」按钮走同一条路，行为一致） |
| 已经在首页 | 弹「要退出 CosPlan 吗？」→ **继续编辑** / **退出** |

- 确认框里写明：**已存的内容不会丢**，只是清缓存、换浏览器会没，建议定期导出备份。
- 选「退出」才真的离开页面。
- 个别内核在 `file://` 下不允许写历史记录 → 自动降级成原来的行为，不会因此卡住或白屏。

---

## 3. 项目与装备：长按 3 秒删除（三级确认）

列表里**按住不放 3 秒**（行底部有一条红色进度条走到头就生效），进入删除流程：

| 第 1 步 | 确认对象 | 「要删的是这一件 / 这一套」+ 名称、状态、价格、挂在几个出装、几条流水 |
|---|---|---|
| 第 2 步 | 删除范围 | 装备：说清会一起清掉什么、不受影响什么。<br>项目：多一个开关 —— **连这套的 N 件装备一起从台账删掉**（默认不勾；不勾就是装备留着、只解除关联） |
| 第 3 步 | 最后确认 | 红字写明删的是什么；**「确认删除」按钮要等 3 秒才亮**，防手滑 |

- 删完的提示条仍带**「撤销」**：整库快照回滚，项目、装备、拍摄单、流水一起回来（4 秒内、期间没有别的改动）。
- 详情页原来的「删除」入口**并到同一套三级确认**，不再存在"一个地方一次确认、另一个地方三次"。
- 长按只在手指上生效；电脑端仍用详情页的「删除」。
- 手指移动超过 12px 就取消（当成滚动）；落在按钮、角标、链接上的起手不算长按。

---

## 4. 出装日（现场模式）

**入口**：首页右上角「出装日」→ 弹出**角色选择卡片**（一行一个角色：项目名、今天/几天后、装备就绪数）→ 点一个进现场页。

这一页只保留**车上、单手、10 秒找到信息、不怕点错**需要的东西：

| 区块 | 内容 |
|---|---|
| 顶部大字 | 角色名（22px）、项目名、**今天 / 几天后 + 几点集合**、场地 + 摄影师 |
| 三个大按钮（52px 高） | **复制地址**（场地名+地址+集合点，一次全复制，粘给高德/微信）· **打电话**（摄影师留了电话才出现，直接拨号）· **拍摄单**（要看细节才跳回去） |
| 出装清单 | 大字进度「**还差 N 件**」（红色）+ 进度条；**整行 64px 高、点一下就勾**，勾上变灰划线，再点一下取消；每行显示**放在哪儿**（衣柜2层左），找东西全靠它；借出去的标红「已借给 谁」 |
| 应急包 / 自加项 | **默认折起来**，不占现场视线 |
| 本场人员 | 摄影师 + 预约过的人：CN、约定时间、报价；**点一下复制电话或平台 ID** |
| 拍后交付 | 约定几张、什么时候给；逾期标红 |

- 打勾**写回同一张拍摄单的清单**，不是另建一份数据 —— 回看拍摄单时勾还在。
- 这个角色这套**还没建拍摄单**：页面给一个大按钮「＋ 建一张今天的拍摄单」，点一下就顺手生成清单，不用跳出去建。
- 角色**还没立项**：给「＋ 新建出装项目」。
- 底部小字提醒："整行都能点：勾上再点一下就取消，不用怕点错。"
- 清空全部打勾放在卡片右上角，且要确认一次（唯一一个要确认的动作）。

---

## 5. 滑动槽加滑轨（电脑端能用了）

之前筛选条只有触摸能横滑，电脑上既不能拖也没有可见的滚动条，**后面的标签根本够不到**。现在：

- **鼠标按住拖动**横向滚（拖过 5px 就不算点击，不会误选标签）；
- **滚轮**在滑动槽上滚就横向滚，滚到头自动交还给页面，不抢正常滚动；
- 桌面端显示**细滚动条轨道**（8px）+ `grab / grabbing` 抓手光标；
- 触摸端保持原样（惯性横滑 + 无滚动条）。

覆盖所有滑动槽：装备类别条、装备筛选条、项目阶段条、通讯录角色条、拍摄单状态条、装备详情的推进条。

---

## 6. 随行改动

| 项 | 说明 |
|---|---|
| 处置字段单一来源 | `itemDisposalFields()` / `itemDisposalRules()` 抽出来，「编辑」表单与详情页快改共用，杜绝两处口径分叉 |
| 删除入口统一 | `delItemConfirm` / `delProjectConfirm` 改为走长按那套三级确认 |
| 长按的吞点击窗口独立 | 只吞浏览器补发的那一次 click（320ms），不像滑动那样用 500ms —— 否则弹窗刚出来时用户点「下一步」会被吃掉 |
| 选角色卡片文案 | 去掉重复的分隔符（原来是 `常驻 · · 10天后`） |

---

## 7. 有意不做 / 边界

1. **不传照片**（沿用）：现场页也只放链接与文字。
2. **去向 CN 仍必填、仍不给手势绕过**：`报废/已售` 与**滑进** `外借中` 只能进表单；快改卡片里也照样拦。
3. **返回键接管靠浏览器的历史记录**：如果内核在 `file://` 下禁止 `pushState`，自动退回原行为（不会白屏、不会卡住），此时只能靠页面内的「‹ 返回」。
4. **长按只在触摸上生效**，电脑端不做（鼠标长按选中文本更常见）。
5. **三级确认只给删除**，普通编辑不加确认，别把"防手滑"做成"什么都要点三下"。
6. **出装日不新建第二套数据**：清单、人员、交付全部读拍摄单与预约单，改一处两边一致。

---

## 8. 自检

- `node _sandbox_test_13.js` → **通过 93 / 失败 0**（快改 17 类入口与落库/撤销/取消、处置字段同源、长按三级流程与连带删除、出装日选项目/选场次/清单/人员/一键建单、返回栈入栈出栈与退出确认、滑轨与滑窗窗口）
- 真实浏览器 375px：长按 3 秒 → 三步弹窗逐级推进、第 3 步按钮 3 秒后才亮、删除后撤销可整库回滚；项目连带删除后 7 件装备 + 拍摄单 + 9 条流水全消失，撤销后全部回来；双击（鼠标）与连点两下（触摸）都能开卡、单击不开；改存放位置只动存放位置；出装日 18 行清单行高 ≥64px、按钮 52px、无横向溢出；鼠标拖动 70px、滚轮 120px、拖后补发的 click 未误选标签；9 个新弹窗内部 0 溢出；XSS 探针在 9 个界面注入 0 处。
- **仍需你在真机确认**：手机返回键的实际手感（浏览器接管历史各家实现略有差异）、长按 3 秒是否偏长、震动反馈。
