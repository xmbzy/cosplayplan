# CosPlan v1.3.3 更新说明

文件：`cosplan-1.3.3.html`（1.3.2 / 1.3.1 / 1.3 / 1.2.2.2 / 1.2.2 / 1.2 / 1.1.2 / 1.1 / 1.0 全部保留不动，日常用这一版）
时间：2026-09-11
一句话：**出装日终于能选到一个角色手里的任意一套出装（你报的那个 bug），另外修掉"关弹窗之后下一次返回会直接退出、不问一句"，并把两处没达标的描边改回 token。**

> 这是 `1.3.2` 的**定向修复版**，不加玩法、不加表。存储键仍是 `cosplan.data.v1`，**直接接着 1.3.2 的数据用**。

---

## 1. 你报的那个 bug：某套出装不显示

**现象**：有时候出装日里找不到某一套出装。你猜"可能和没给那套项目安排时间有关"——**方向是对的，而且比想的更严重**。

**实际有三层原因叠在一起：**

| # | 原因 | 后果 |
|---|---|---|
| 1 | 出装日取"这个角色的哪一套"时，**只按「计划完成日」排序取第一个**，没填的当成 9999 年排到最后 | 没填截止时间的那套被挤到后面；**只要同一角色有另一套填了时间，它就永远出不来** |
| 2 | 排序**完全不看拍摄单日期** | 一套"已经约了下周三拍摄、但没填计划完成日"的出装，会被一套"只随手填了个截止日"的挤出局 |
| 3 | 选择卡**只有一层、按角色列**，一行只写一套的名字；顶栏「换一套」打开的还是这张角色卡 | 一个角色有多套时，**除了被自动选中的那一套，其余的没有任何入口能进去** —— 不是排得靠后，是根本到不了 |

**改法：**

1. **列不列得出来，与填没填时间彻底脱钩**。新函数 `onsiteProjectsOf(角色)` 返回这个角色的**全部**出装，一个都不过滤；排序只决定"默认先看到哪一套"：
   `有还没到的拍摄` → `只有计划完成日 / 只剩过去的拍摄` → `什么都没排（按最近改动）` → `已出片`（永远最后，但**照样列出**）。
2. **选择卡拆成两层**：先选角色（一个角色有多套的，行右边直接写「**4 套 ›**」），再选"这个角色出哪一套"。第二张卡把这个角色的全部出装一行一套列出来，写清 **阶段 · 拍摄时间或「没排拍摄」 · 装备就绪几件 · 皮肤**，当前那套标「当前」，底下有「**换别的角色**」能退回上一层。
3. **只有一套的角色照旧一步就进**，不多问一次——不为了这个 bug 给所有人加一次点击。
4. 现场页按钮区在多于一套时多一个「**换一套**」，和右上角顶栏那个「换一套」是同一个入口（顶栏那个现在会**直接开到出装清单**，不再绕回角色卡）。
5. 你手选的那一套记在 `S.onsitePid` 里；那套后来被删掉或改了角色，自动回落到默认那套，不会白屏。

---

## 2. 返回键：关掉弹窗之后，下一次返回会直接退出（不问）

1.3.1 那条"弹窗开着时返回先关弹窗"是对的方向，但**关弹窗那一次返回顺手消费掉了一条浏览器历史，却没补回来**，而页面栈里仍然留着当前这一屏 → 两边错开一层 → 等你走到首页再按返回，就**直接退出应用，不弹「要退出 CosPlan 吗？」**。这条恰好是"返回键接管"要防的事。

真实浏览器复现（1.3.2，一次 back ≈ 手机上按一次返回）：

```
开着一张快改卡，人在装备详情：
  #1 关弹窗（对）→ #2 回首页（对）→ #3 离开文档 ⚠ 没有确认框
同一条路径但没开弹窗：
  #1 回首页 → #2 弹「要退出 CosPlan 吗？」      ← 有弹窗时少了一次"问"
```

**修法一行**：弹窗分支 `closeModal()` 之后补 `navGuard()`，把守门员条目垫回去。修完：`#1 关弹窗 → #2 回首页 → #3 弹确认`，全程 0 次离开文档；没开弹窗的路径行为不变。

> 为什么之前没测出来：`qa/` 里**没有任何一条断言提到 `MODAL_OPEN`**，而且沙箱用的假 `history` 不会模拟"退到文档之外"，这条只能在真实浏览器里连按返回才看得见。已补 `qa/r133_check.js` 的真实 `history.back()` 断言。

---

## 3. 两处描边没达标（1.3.2 遗留）

| 位置 | 1.3.2 实测 | 现在 | 说明 |
|---|---|---|---|
| 状态徽章内环 | **1.38:1** | `var(--key-edge)` → **3.52:1** | 1.3.2 写的是 `rgba(32,34,44,.10)`，等于"看着有个边、其实没有"；低对比度截图前后已对照 |
| 危险按钮描边 / 底座 | **2.36 / 2.03:1** | `--key-danger-edge:#c8324a` / `--key-danger-ledge:#a8273c` → **4.81 / 6.38:1** | 原来是硬编码字面色，全站唯一不到 3:1 的键边，而且**不跟随系统「提高对比度」**；改成 token 后一并纳入那一档 |

危险按钮是删除三级确认里唯一"往前走"的按钮，它的边界尤其该稳。

---

## 4. 有意不做 / 边界

1. **不改数据**：`S.onsitePid` 只是视图状态，不落库；装备归属、清单、流水一律照旧。
2. **不藏任何一套，但也不给"什么都没排"的特殊照顾**：它只是排在最后，仍然一行一套列出来。
3. **归属转移仍然不是一个按钮**：改「用在哪个出装」照旧只补记没记过的那笔，已记在某个项目头上的流水不动（1.3.1 的锁定语义）。真要搬，得是显式操作 + 二次确认，留给后续版本。
4. **不传照片**（沿用）：现场页只放链接与文字。
5. **去向 CN 仍必填、仍不给手势绕过**（沿用）。
6. **量到但没动**（都不是 1.3.x 引入，改了会牵动布局，先报给你）：
   - 分页签 `.tabs` 是 `sticky;top:52px`，而顶栏实测 65.5px 高 → 滚动时页签顶部 13.5px 被顶栏盖住（含新加的顶部高光那一条，以及那一小条点击区）。改 `top:66px` 就好，一句话的事，等你决定。
   - 提示条上的「撤销」键 31px 高，比别的键矮 5px（它只在 4.6 秒内存在）。
   - 长按宿主行禁了文本选择（1.3.1 为了不被系统菜单打断），所以**列表行里的文字长按不再能选中复制**；详情页不受影响。
7. `1.3.2` 及更早文件**一字未动**，随时可回退（回退前先导出备份）。

---

## 5. 自检

全部在 `cosplan-1.3.3.html` 上跑：

| 项 | 结果 |
|---|---|
| 版本自检 `_sandbox_test_133.js` | **通过 93 / 失败 0** |
| 逻辑断言 `qa/z133_k13_logic.js` | **通过 77 / 失败 0** |
| 96 条旧版回归 `qa/z133_y13_regress_96.js` | **通过 96 / 失败 0** |
| **返回键真实 history** `qa/r133_check.js` A 段 | **7/0**（1.3.2 对照确实会静默退出；1.3.3 三次返回后弹确认、**0 次离开文档**） |
| **出装日多套** 同文件 B 段 | **14/0**（旧版只认 pY；新版四套全列出、排序 `pX,pY,pZ,pW`、两层卡、没排拍摄与已出片都在、选第三套进的就是那一套、顶栏换一套标「当前」、单套一步进、删当前套自动回落） |
| 描边 token 同文件 C 段 | 3/0（徽章内环 3.52、危险键 4.81，且「提高对比度」下整体再压深一档实测生效） |
| 注入与溢出 同文件 D 段 | 2/0（角色名/出装名/装备名注入 **0 处**；9 个视图 375px 横向溢出 **0**） |
| **现场页端到端** `qa/r133_shots.js` | **6/0**（选到"什么都没排"那套 → 一键建今天拍摄单 → 生成清单 13 行、行高全 64px、勾选框 36px → 点一下勾上并写回 `session.checklist` → 行中心命中自己），**0 JS 报错** |
| 1.3.2 外观复核 `qa/r132_probe.js` | 分页签切换时 `.tabs` 高度与下方内容位移 **0px**（新描边没引起抖动） |
| 归属锁定 `qa/lead131_lock.js` | 4/4（null 补一次 / 重建保归属 / 真界面改关联后流水仍锁原项目 / 手动账不动） |

对照截图在 `qa-screenshots/`：`R133-pick-char.png`（第一层角色卡）、`R133-pick-proj.png`（第二层出装清单）、`R133-onsite-pZ.png` / `R133-onsite-checked.png`（现场页与打勾）、`R133-badge-13.2-low.png` vs `R133-badge-13.3-low.png`（徽章描边前后）。

**仍需你在真机确认**：手机返回键接管后的实际手感（各家内核不同）、长按 3 秒是否偏长、新描边在你的屏幕/户外光下够不够又会不会偏重。
