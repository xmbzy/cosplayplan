# CosPlan v1.5.0 更新说明

文件：`cosplan-1.5.0.html`（1.4.1 / 1.4.0 / … / 1.0 全部保留不动，日常用这一版）
时间：2026-09-12
一句话：**四件新功能——① 分享卡（尺寸卡/集合信息卡一键发给毛娘妆娘摄影师）② 借入追踪 ③ 预算超支预警 ④ 出装复盘报告——外加 Capacitor APK 迁移预研（`Capacitor迁移准备.md`）。**

> 存储键仍是 `cosplan.data.v1`，**直接接着 1.4.1 的数据用**。新字段：装备 `borrowFrom/borrowDue`（借入），读档自动补默认；预算预警复用 `confMute` 忽略机制，零新表。

---

## 1. 逐条说明

### ① 分享卡（把已有数据"变现"成沟通）

- **尺寸卡**：「我的 → 我的数据」卡新增**「复制尺寸卡 · 发给毛娘/妆娘」**——把假毛数据（只发已填的：头围1/头围2/脸长/三庭/眉间距/眼长/眼角到脸边）+ 特殊脸型备注 + 自定义 + 一句话备注整理成一段文本，粘进微信就能用；身体数据（体重三围）**不自动外发**。
- **集合信息卡**：拍摄单页与出装日卡片新增**「分享」**——集合日期时间、场地（含地址交通）、摄影、当天妆造/毛发安排（从预约单汇总）、时间线、雨备、**装备清单（带勾选状态 ✅/⬜ 与借入警示）**、交付约定张数与期限、备注，一段全齐。
- **通道**：`shareText()` 唯一出口——有 `navigator.share` 走系统分享（**这就是给 Capacitor 留的缝**，迁移时换成 `@capacitor/share` 即为原生），没有就一键复制（带"长按复制"降级框）。
- 长图卡片未做（文本卡已覆盖微信场景，canvas 长图列在 Capacitor 准备文档的"有意没做"）。

### ② 借入追踪（借别人的同样高频）

- 装备表单新增「借入」组（来源选"借用"或已填出借人时出现）：**借谁的 CN + 约定归还日**；装备详情可双击"借入"行快改。
- **四处警示**：装备详情"借入"行；拍摄单清单徽章「借自 X·应还 Y」；**出装日现场清单**「⚠ 借自 X，应还 Y，别弄坏」（黄色徽章）；集合信息卡里每件借入件自动带警示。
- **首页提醒**：归还日前 7 天起到逾期，时间线出现「借入到期：该还了」。
- 与"外借中"（自己的东西借出去）完全独立，互不干扰。

### ③ 预算超支预警（复用冲突预警机制，零新表）

- `conflictScan()` 新增第 ③ 类：花费达预算 **90% → 「预算告急」**（弹窗给出百分比与剩余额度）；**超过 → 「预算超支」**。
- 忽略按**里程碑 key**（`bc:项目:90` / `bc:项目:over`）：忽略"告急"后，真超支时仍会提醒一次。
- 与人员撞档/装备赶不上共用同一弹窗（正中间、可逐条忽略、可全部忽略），「我的 → 档期冲突预警」里统一管理与清空。

### ④ 出装复盘报告（闭环记忆）

- **触发**：项目**首次**转「已出片」（含滑动推进）——先弹"这套装备怎么处理"（老流程）；没有可处置装备时直接弹**出装复盘报告**；已归档的项目页常驻「出装复盘报告」卡，随时查看。
- **内容（全部实时计算，不落库）**：
  - 总成本（预算对比/超支）、出物回血与净成本；
  - **装备利用率**：上场 X/Y 件——口径是"清单里被**勾选过**"的件（清单自动包含全部在手件，光在清单里不算用上），并列出没上过场的名单；
  - 拍摄完成场次与跨度、交付逾期/未返图统计；
  - **翻车记录**：仍停"修改中"的装备、取消/改期场次、超支金额（没有则夸一句）；
  - **下次改进清单**：闲置件提示、愿望单遗留、催片时机、处置去向查漏——没有可改进的就建议留着联系方式下次直接约。
- 报告弹窗内**复制**或**分享**（走 shareText 同一通道）。

## 2. 兼容与数据

| 变更 | 策略 |
|---|---|
| 装备 `borrowFrom/borrowDue` | migrate 补空串 |
| 预算预警 | 读 `projects.budget`（库里早就有）+ 流水合计，零新字段 |
| 复盘报告 | 实时计算不落库（先补记数据再看报告更准） |
| Capacitor | 不改产品代码；`shareText` 是原生分享的唯一替换点，见 `Capacitor迁移准备.md` |

## 3. 自检（`qa/run_all_150.js`）

| 套件 | 结果 |
|---|---|
| `_sandbox_test_150.js`（1.3 基线） | **94 / 0** |
| `qa/z150_k13_logic.js` | **77 / 0**（QUICK_FIELDS 目录 22 个入口：21 + borrow） |
| `qa/z150_regress_96.js` | **96 / 0** |
| `qa/z150_audit.js`（一致性 + 口径扫描） | **61 / 0** |
| `qa/g150_deadbtn.js`（死按钮扫描） | **2 / 0** |
| **`qa/g150_features.js`（新功能沙箱守卫）** | **22 / 0**：尺寸卡只发已填假毛字段且拦截空卡、集合卡时间/清单勾选/借入警示/交付齐全、navigator.share 缝、借入四链路（详情/清单/现场/到期）、预算 90% 与超支两档里程碑忽略、复盘数字（利用率=勾选口径）与归档两条触发路径 |
| **`qa/g150_browser.js`（真 Chromium）** | **9 / 0**：启动自动弹预算告警（90%）→ 忽略持久化；尺寸卡/集合卡真实点击生成文本（强制复制降级框断言内容）；清单与现场借入警示；归档→处置→常驻复盘卡全链路；0 JS 报错 |

合计 **361 断言 / 0 失败**；截图 5 张 `qa-screenshots/G150-*.png`。

**仍需你真机确认**：① 分享卡文本格式在微信里粘出来的观感（emoji 与缩进是否要调）；② 借入到期"提前 7 天"合不合适；③ 预算 90% 阈值与措辞；④ 复盘报告的"利用率=勾选过"口径是否符合你对"用上"的理解。

## 4. 新增/变更文件

- 产品：`cosplan-1.5.0.html`（由 1.4.1 逐条 Edit 而来）
- 套件：`_sandbox_test_150.js`、`qa/z150_k13_logic.js`、`qa/z150_regress_96.js`、`qa/z150_audit.js`、`qa/g150_deadbtn.js`、`qa/g150_features.js`、`qa/g150_browser.js`、`qa/run_all_150.js`、`qa/setup150.js`
- 文档：**`Capacitor迁移准备.md`（新）**、`更新说明-1.5.0.md`（本文件）、`使用说明.md`（同步）
