# CosPlan v1.5.2 更新说明

文件：`cosplan-1.5.2.html`（1.5.1 / 1.5.0 / … / 1.0 全部保留不动，日常用这一版）
时间：2026-09-12
一句话：**时装名默认值修正（只有新建角色默认"原皮"）；重拍检测（同角色同时装再立项自动命名"第 N 次拍摄"并继承关联装备）；出装日选套只显示时装名；自加项标明"点击折叠"；清单滑动手势（右滑勾/取消、左滑移除、可撤销，装备行不可移除）。**

> 存储键不变，直接接着 1.5.1 的数据用。零新表零新字段。

---

## 1. 逐条对照（用户意见 → 实现）

| 意见 | 实现 |
|---|---|
| 选已有角色时时装名不该默认"原皮"，只有新建角色才默认 | `derive` 重写：`characterId==='__new'` 且为空 → 填"原皮"；已有角色 → 保持空白；**从"新建角色"切回已有角色时，自动填上的"原皮"会被清掉**（用户手动编辑过 = `onTouch` 置 touched，之后一概不动，包括故意给已有角色手填"原皮"） |
| 同角色+同时装再立项：自动加"（第二次拍摄）"，装备默认同前，人员不默认 | `formProject.onSave` 新增重拍检测：按 `characterId+skin` 找已有项目，取最近更新的一套为"上一套"——① 项目名自动拼 `作品·角色·时装（第 N 次拍摄）`（N 用中文数字 `cnNum`，二/三/…/十一/…，自定义项目名时不加后缀）；② **关联装备继承**：上一套挂载的装备自动挂到新项目（`syncItemExpense` 重跑但"非空即锁定"保证钱不动）；③ **人员不继承**：预约单/拍摄单本来按项目隔离，新项目从空开始；toast 会说明"已继承「上一套名」的关联装备" |
| 出装日选套只显示时装名 | `onsitePickProj` 行标题改为 **`p.skin`（时装名）**，项目全名退到副行（`阶段 · 时间 · 装备就绪 · 项目名`），多套同角色时一眼可辨 |
| 自加项要有明确的"折叠"字样 | 现场页应急包折叠组摘要改为「应急包 / 自加项　N 项 · **点击折叠**」 |
| 所有清单：右滑挑勾（再右滑取消），左滑删除（装备不可删），删除可撤销 | 滑动引擎新增 `check` 类型（`data-sw="check:拍摄单id\|行id"`），**现场页与拍摄单的清单行全部接上**：右滑（灰色面板）= 勾上 / 再右滑取消；左滑 = 移除——**装备行左滑不可用**（`itemId` 行无删除动作，避免刚滑完就被"未同步"加回来的死循环），应急包行与手动行可移除（应急包行照旧记入 `kitOff`）；移除与勾选都走 `applyAction`，**4 秒内可点「撤销」**。内部把删除逻辑抽成 `removeCheckRow`（✕ 按钮与滑动手势共用一份） |

## 2. 兼容

- 零新表、零新字段；重拍检测只读 `characterId/skin/updated_at`。
- 清单行包裹进滑动结构后，原有的整行点击打勾、✕ 删除、应急包"点击折叠"记忆（`S.osKitOpen`）全部保留。

## 3. 自检（`qa/run_all_152.js`）

| 套件 | 结果 |
|---|---|
| `_sandbox_test_152.js`（1.3 基线） | **94 / 0**（版本戳对齐 1.5.2） |
| `qa/z152_k13_logic.js` | **77 / 0** |
| `qa/z152_regress_96.js` | **96 / 0** |
| `qa/z152_audit.js`（一致性 + 口径扫描） | **61 / 0** |
| `qa/g152_deadbtn.js`（死按钮扫描） | **2 / 0** |
| **`qa/g152_features.js`（沙箱守卫）** | **19 / 0**：时装名 derive 四态（已有空白/新建原皮/切回清空/touched 不动+手填原皮不被清）；重拍五断言（第二次命名、装备继承、人员不继承、第三次、自定义名尊重）+ cnNum；选套标题=时装名；「点击折叠」；手势六断言（data-sw 覆盖、装备行左滑禁用、右滑勾/取消翻转、应急行左滑移除+kitOff） |
| **`qa/g152_browser.js`（真 Chromium）** | **13 / 0**：表单真实切换三种路径的时装名值；真实保存 → 项目名带（第二次拍摄）且装备继承；选套弹窗标题=时装名；两处清单 data-sw 全覆盖 + 步骤函数真实执行（勾上→取消→移除→kitOff）；折叠组"点击折叠"可见；0 JS 报错 |

合计 **362 断言 / 0 失败**；截图 4 张 `qa-screenshots/G152-*.png`。

**仍需你真机确认**：① 清单滑动手感——右滑勾/左滑删的力度阈值（`SW_MIN=46px`）合不合手；② 滑动与整行点击是否有过误触（理论上引擎会吞滑动后的那次 click）；③ 重拍检测按"角色+时装"匹配，若你习惯同时装跨作品复用（联动皮肤）再说一声可调整匹配键。

## 4. 新增/变更文件

- 产品：`cosplan-1.5.2.html`（由 1.5.1 逐条 Edit 而来；你已把上版更名 1.5.1，本版顺延）
- 套件：`_sandbox_test_152.js`、`qa/z152_k13_logic.js`、`qa/z152_regress_96.js`、`qa/z152_audit.js`、`qa/g152_deadbtn.js`、`qa/g152_features.js`、`qa/g152_browser.js`、`qa/run_all_152.js`、`qa/adapt152tests.js`
- 文档：`更新说明-1.5.2.md`（本文件）、`使用说明.md`（同步）
