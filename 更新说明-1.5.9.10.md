# CosPlan 1.5.9.10 更新说明 · 回补 1.5.9.9 的两条：草稿不许说谎、弹窗滚动要有身份

> 交付物：`cosplan-1.5.9.10.html`（660,667 字节 / 9,461 行 / CRLF / md5 `d00b6710c5bbd39b3e7945453223cd17`）—— **没出 APK**，手机上仍是 `release-kit/CosPlan-v1.5.9.5-release.apk`（1.5.9.6 ~ 1.5.9.10 五版都只动 HTML；要出包说一声，versionCode 走 15906）
> 基线：`cosplan-1.5.9.9.html`（md5 `287c988118bc9f56e204f816029ebbdd`，冻结不动）
> 输入：《1.5.9.9-评审报告》P2-1 / P3-1 / P3-2 / P3-3
> 你拍板的两件事：**草稿的处理 = 加一个显式的「存下来」出口**（不是自动存，也不是只改文案）；**这一版只收 P2-1 + P3-1**，P3-2（断言绕过 UI）与 P3-3（一处没用上的赋值）留着不动

---

## 0. 一句话与一张表

**上一版那句「UI 态、不落库」我写得理直气壮，界面却用「草稿在」三个字承诺了相反的事 —— 这一版把谎改掉，再给一个"要留就显式存"的出口；顺手把弹窗滚动恢复补上身份判断。**

| 项 | 上一版 | 本版 | 说明 |
|---|---|---|---|
| 产物 | 657,584 B / 9,435 行 | **660,667 B / 9,461 行** | 9 条补丁，OLD 侧按行号从 1.5.9.9 切（每段带首行 marker 断言）；两次 `--force` md5 相同 |
| 产物自检 | 75 条 | **75 条 / 0 FAIL** | 本版没有需要新钉的算法口径；六条判据全落在门禁与端到端 |
| 内联 JS 字符串白名单 | 131 处 | **131 处（新增 0 处）** | 新代码一颗内联 JS 插值都没加 |
| 独立门禁 `gate.js` | 125 项 | **131 项** | 8j) 段 6 条，逐条负向验证 **6/6**；跑 1.5.9.9 仍 125、9.8 仍 112、9.7 仍 100、9.6 仍 86、9.5 仍 69、9.4 仍 54 |
| 本版端到端 | 19 条 | **13 条 / 0 未过**（`qa/x15910_draft.js`） | A 角标说实话 / B 显式存进发布记录（含"平台没选就拦住"）/ C 不点就不落库 / D 弹窗滚动身份校验 / E 走查 |
| 全量 | 29 套 / 1060 条 | **30 套 / 1073 条 / 0 失败** | 29 份派生副本**逐套同数** = 零回归；1060 + 13 = 1073 |
| 派生副本的断言改写 | 13 条 | **3 条**（本版，全部走 `apply15910.js` 的 `EXPECT` 表） | 1.5.9.9 那 29 套原件一字未动 |
| 数据模型 | — | **零变更** | 草稿仍只是 `S.ff`；「存下来」写进的是**已有的** `publications`（`caption` / `platform` / `date` / `projectId`），不加字段 |

---

## 1. P2-1：那句谎话，和一个不猜用户的出口

**评审说得对，而且我给的理由站不住**：我写「UI 态、不落库、不进备份」是想守住"备份里不掺临时物"，但卡片角标写的是**「草稿在」**——用户读到的是"你替我存着了"。实测一次刷新，`S.ff` 回空、角标回「还没写」，用户自己打的那段字蒸发且不吭声。那是整个功能唯一的产出物。

按你选的"**加一个显式的存下来出口**"落地：

| 处 | 改了什么 |
|---|---|
| 角标 | `草稿在` → **`这次写的，没存`**；卡片正文补一句「**这份草稿只在这次打开的页面里，刷新就没了** —— 要留在…」 |
| 复制模态 | 原来那句「这段只在你这台设备上」补成「**而且关掉重开就没了**：要发就点「复制这段」，要留着就点「存进发布记录」」 |
| 新按钮 | 「**存进发布记录**」→ 走**原有的** `formPub(null,preset)`：正文进「文案 / 草稿」那一格，日子与挂哪套一起带过去；你确认保存才落库，之后在那套出装的「拍摄」页发布记录里能看到、点进去能改，也**跟着你的备份走** |
| 不点它 | 什么都没发生：草稿只在内存（端到端 C1 同时搜 `localStorage` 与 `buildExport()` 两处，都搜不到那段字） |

**顺带挖出一条我自己写进去的静默**（评审没抓到，是端到端 B2 逼出来的）：我原本想"平台留空由你选"，但 `formPub` 的平台是 `req:1` 且下拉没有空选项 —— 传 `platform:''` 会被浏览器**兜到第一项「微博」**。也就是说：用户没做的选择，被界面替他做完了，而且落库时看不出来。改法：**值为空时给一格「— 选平台 —」**，不选就保存不了并明说「请填写：平台」；从「＋ 发布」正常新建时 `cur.platform` 已有默认「微博」，那一格不出现，**旧行为一字不变**。门禁 P2-1e 钉住这三件事（有空格 / 仍必填 / 默认值路径不变），负向验证里"把它改回 `opts:PUB_PLATFORMS`"必须红。

---

## 2. P3-1：弹窗滚动恢复要有身份

上一版 `openModal` 只判"上一张窗还在不在"就恢复滚动 —— 于是**从一张滚到底的窗里点开另一张长内容的窗，会开在半中间**。评审很诚实：他把我能想到的四条路径全跑了一遍，**当前界面里没有一条真会咬到用户**，所以只给 P3，但"今天不咬人，明天谁加一张长内容弹窗就会"。

改法照主滚动的 `__cpSig` 同一思路：**标题一致才恢复**。

```js
var _mb=document.querySelector('.modal-body'); var _keepMB=_mb?_mb.scrollTop:0;
var _mbT=_mb?(...取 .modal-head h2 的文本...):'';
if(_mb) setTimeout(function(){ /* 新窗标题与旧窗一致才把位置还回去 */ },0);
```

端到端 D1 / D2 两侧各一条：**同一张条款面板改一处选择重开 → 保住 200px**（这正是你要的"不闪回顶上"）；**从已滚动的面板点开另一张（标题不同的）发布记录表单 → 从头看**。D3 顺带确认 1.5.9.9 的主滚动行为没被带坏。

---

## 3. 被改写的测试断言（本版 3 条，全部披露）

| # | 哪条 | 为什么必须改 | 变严还是变松 |
|---|---|---|---|
| 1 | `r15910_ui` 第 47 条逐屏文案白名单 | 「我的」页角标改口，多出「这次写的」这个新词 | **只加一个词**；"某条记录从页面上消失"那个方向照旧红 |
| 2 | `x15910_free` 文件头 | 机械换号把"v1.5.9.10 端到端：…自由行通道"写进上一版那套的标题，像是本版新写的 | **判据一字未动**，只把标题改成"1.5.9.9 收尾轮…本版当回归跑" |
| 3 | `x15910_free` 结尾统计行 | 同上（换号带歪版本归属） | **判据一字未动**，只改标签 |

**没有一条是放松判据。** 另外本版端到端里有一条值得单说：`x15910_draft.js` 的 B3 一开始断言"发布记录列表里能搜到那段正文"，实测 `false` —— 查下来是**列表行本来就不显示 caption**（显示平台徽章 + 日期 + 链接/统计）。这是断言选错了证据，不是产品缺陷：改成"行存在（平台徽章在）+ 点进去 `#fld_caption` 等于原文"。同一条里还顺手暴露了项目页 tab 的入口形状（`go('project',{tab:'shoot'})` 不设置 `S.projTab`，要显式赋），这是老坑，我这次又踩了一遍。

---

## 4. 本版我自己写坏、被抓住的几处

1. **界面替用户做选择**（`platform:''` 被下拉兜成「微博」）—— 上一版刚立过"不猜领域内容当默认值"，这版差点亲手违反，而且是**在写"我不猜"的同时猜了**。判据补进 §21.11。
2. **门禁 P2-1a 一开始把自己钉红了** —— 判据写成"全文件不许出现『草稿在』"，可我在版本沿革里正要引用那句谎话。改成**剥掉块注释再查**（与 1.5.9.9 沙箱那条同一手法）。
3. **端到端两处 Node / 页面作用域搞混** —— 在 Node 侧写了 `addDaysISO(30)`（页面函数）直接 `ReferenceError`；B3 断言拿"列表里搜正文"当证据（列表本来不显示正文）。
4. **派生构建脚本又踩一次换号顺序** —— 我先按"改后"的样子找换号链终点锚点（`run1599_full` 已被前一步改成 `run15910_full`），命中 0 次才想起 `repAll` 的先后；另外 EXPECT 锚点我按记忆写成"六件顺手"，实际文件里是"六件使用顺手"，**咬空即失败的检查第三次替我拦住一条静默不生效**。
5. **`_doc1599.js` 那两条脚本教训已记**（数组后紧跟 `R.forEach` 必须写分号；替换项里别误多打一个 `[`）。

8. **交付前看图又抓出自己一处方向文案错**：卡片写「要留在**「我的 → 这套出装的拍摄页」**的发布记录里存一条」，可发布记录在**「项目 → 那套出装 → 拍摄页」**，不在「我的」下面 —— 一句指路的话写错，等于把刚修好的"说实话"又搅浑一次。改成「要留就在「项目 → 那套出装 → 拍摄页」的发布记录里存一条（点下面「存进发布记录」那颗）」，重建后 gate 131 / 负向 6/6 / 本版 13 条 / `r15910_ui` 49 条全绿，最终全量 30 套 1073 条 0 失败（md5 因此变成 `d00b6710…`）。

---

## 5. 已知限制 / 继续挂着的账

- **P3-2（断言绕过 UI）**：`x1599_terms` 里"填圈名"从"在框里打字"降级成 `setMyCn('阿棠')`。你选了这版不动，记在账上；将来补一条"从「我的」页改圈名 → 条款抬头跟着变"的正向链路（本版端到端没补，`x1599_free` 的 D 段只覆盖了 COSPIC1 那条）。
- **P3-3（一处没用上的赋值）**：`formFreeform` 的 `onSave` 里 `var d=freeformDraft();` 之后没用到。按你的选择没删，留着。
- **草稿本身仍不落库**：这是决定，不是遗漏。要留只有两条路——复制出去，或点「存进发布记录」。
- **`termsParam` 那笔老账**（评审 P3-3 上一版）仍按"不改行为、写进《使用说明》边界"处理。
- **APK 已五版没跟上**（1.5.9.6 ~ 1.5.9.10 全纯 HTML）：手机用户这些新东西一个也没拿到；真机四项（微合同出图落盘 / 装包单落盘 / 通知调度 / release 关 WebView 调试）仍待你装机回说。

---

## 6. 交付清单

- `cosplan-1.5.9.10.html` —— 产物（660,667 字节 / 9,461 行 / CRLF / md5 `d00b6710c5bbd39b3e7945453223cd17`）
- `qa/_15910_new.txt` + `qa/_15910_patches.txt` + `qa/_mk15910_patches.js` + `qa/_mk15910_apply.js` + `qa/apply15910.js` —— 构建（NEW 侧原文 / 9 条补丁 / 按行号切 OLD 且带首行 marker / 由 apply1599 派生构建脚本 / 75 条自检 + 派生 29 份副本 + EXPECT 3 条咬合核对）
- `qa/gate.js` —— 独立门禁 131 项（8j) 段按版本启用）；`qa/_gate15910_neg.js` —— 6 条负向验证
- `qa/x15910_draft.js` —— 本版端到端 13 条；`qa/run15910_full.js` —— 全量 30 套；`qa/_r15910_full.log` —— 原始输出
- `使用说明.md`（表头 + 本版说明 + 新增「自由行申报文案」一节 + 本版边界）、`开发交付文档.md`（§21 总账 + §21.11 本版立的规矩）、`更新说明-1.5.9.10.md`（本文件）
- 输入文档（未改）：《1.5.9.9-评审报告》
