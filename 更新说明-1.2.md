# CosPlan v1.2 更新说明

- 文件：`cosplan-1.2.html`（由 1.1.2 逐项加装而来，仍是单文件、零依赖、无构建）
- **数据完全兼容**：存储键 `cosplan.data.v1` 未动，1.1.2 打开 1.2 直接接着用，不迁移、不清洗；1.0/1.1 备份导入照常自动转换
- 用户诉求原话：**"更便捷（手机上左滑右滑调状态）、内部联动更多、更适合手机操作、整体更简洁但功能强大"**
- 验证：新增 Node 沙箱断言 78 条（**78 通过 / 0 失败**）；1.1.2 回归集跑 1.2 与基线持平（**无新增失败**）；真实浏览器用合成 PointerEvent 走完整条手势链 + 375px 同源 iframe 布局量测，**控制台 0 条输出**

---

## 一、滑动手势（本次核心）

### 1.1 能滑什么

| 对象 | 右滑（退） | 左滑（推） | 说明 |
|---|---|---|---|
| 装备（台账行 / 项目装备行 / 借出去的行） | ‹ 退一格 | 推一格 › | 11 个状态可滑；`已售出 / 已报废 / 已归还` 与「滑进外借中」**故意不给滑** |
| 项目卡片（项目列表整卡） | ‹ 上一阶段 | 下一阶段 › | 滑到「已出片」会立刻弹出这套装备怎么处理 |
| 拍摄单 | ‹ 退一格 | 推一格 › | `已取消 / 已改期` 要填原因，不在流程里 → 滑不动 |
| 预约单 | ‹ 退一格 | 推一格 › | 待确认 → 已定档 → 已付定金 → 已完成 → 已结清 |
| 交付物 | ‹ 退一格 | 推一格 › | 未交付 → 已交付 → 已选片 → 修图中 → 已返图 |
| 待办 | ‹ 重新打开 | 完成 › | 整行可滑，也可以直接点 |
| 项目详情空白区 | ‹ 前一分页 | 后一分页 › | 4 个分页横向翻，不用瞄准小标签 |

### 1.2 判定与手感参数

| 项 | 取值 | 为什么 |
|---|---|---|
| 触发阈值 / 翻页阈值 | 行 46px / 分页 64px | 行要灵敏，翻页要更明确，免得误切 |
| 最大位移 | 88px（`swClamp` 钳位） | 底衬面板用 999px + `.swin{overflow:hidden}` 裁切，滑到哪都不会漏边 |
| 方向锁 | 位移 >8px 后判定，`|dy| > |dx|×1.4` 立即放弃手势 | 上下滚列表时绝不被抢走 |
| 事件模型 | Pointer Events，只认 `touch`/`pen`，`mouse` 直接 return | 电脑上仍然是点，不受影响 |
| 竖向冲突 | `touch-action:pan-y` + `passive:false` 的 `touchmove` 拦截 | 锁定后浏览器不再顺手竖滚 |
| 误点保护 | 提交后 500ms 内吞掉补发的 `click`，**但 `#toast` 放行** | 否则刚滑完点「撤销」会被一起吞掉（实测踩过） |
| 起点豁免 | `swSkip()`：落在 `[onclick],input,button,select,textarea,a,label` 上不算滑动 | 点删除角标 / 勾清单 / 复制链接时不误触 |
| 反馈 | `navigator.vibrate(12)`（try/catch 包裹） | 支持的安卓机震一下，不支持的静默 |

### 1.3 撤销

`applyAction()` = 改前 `JSON.stringify(DB)` 整库快照 → 改 → 落盘重绘 → 弹 4.6 秒「已…〔撤销〕」。
撤销只在 `WRITE_SEQ`（每次 `save()` 自增的写计数）没变时生效，否则提示「撤销已失效：这之后又有新的改动」。
选整库快照而不是反向操作，是因为一次滑动会连带产生自动流水、自动转阶段、自动补日期——快照能全部一起回滚，不用给每条副作用写反向代码。

> 撤销**专供给滑动**（外加待办勾选、清单/联动的一键动作）。点具名按钮（详情页「推进到 XX」、阶段 chip）沿用原来的普通提示，不给撤销——那类操作反悔路径就在同一排按钮上，再点一下就是。

### 1.4 与状态机的关系（重要）

**滑动不是新开的口子**：`ITEM_SWIPE_NEXT/PREV` 与三条 `*_FLOW` 全部是 `STATUS_NEXT` 的合法边，为此给状态机补了 6 条回退边：`计划中→愿望单`、`已下单→计划中`、`已到手→在途`、`制作中→计划中`、`已完成→制作中`、`可用→试装通过`。
滑动表是状态机的**子集**（沙箱逐条比对过），所以不会出现"滑得过去、点不到"或者"点得到、滑不动"两套口径；表单下拉里仍保留「强制跳转（录错纠正用）」做兜底。
详情页的「退回 XX」chip 加了虚线样式（`.chip.back`），与实心「推进到 XX」一眼分开。

---

## 二、内部联动（以前要自己想起来记，现在顺手带出来）

| # | 触发 | 自动发生 |
|---|---|---|
| 1 | 拍摄单推到「已确认 / 已完成」 | 项目若还停在「计划中」→ 自动转「拍摄中」；滑动提示文字里预先写明「· 项目自动转拍摄中」 |
| 2 | 项目推到「已出片」（滑动或点） | 立刻弹出「这套 N 件怎么处理？」，逐条点进去就是**已预置「已售出」的装备表单**（去向 CN 必填仍在）；不想处理点「这次全部留着」 |
| 3 | 装备滑到「已下单 / 已到手」 | 缺日期就自动补今天 → 自动费用流水不再落在无日期的旧数据上 |
| 4 | 交付物离开「未交付」 | 自动填实际交付日；退回「未交付」则清掉，逾期红字不会一直挂着 |
| 5 | 交付物逾期（首页红区） | 行尾「＋转待办」一键生成催片待办（截止 3 天后，带 `refType/refId`），**同一条重复点会提示已转过** |
| 6 | 拍摄单已完成但没有任何交付物 | 显示「＋ 记一条原片交付（默认 7 天内返片）」，一键建 `原片/未交付/agreedDate=今天+7` |
| 7 | 拍摄单「安排」卡 | 「按预约生成时间线」→ 抓同项目当天预约单 + 开拍时间拼成 `14:00 妆娘 小满 → 15:30 开拍`；已有时间线则变「重新生成」。类型名已在名字里时不重复拼（曾出现"妆娘 妆娘·小满"） |
| 8 | 项目装备变动 / 处置后 | 出装清单顶部提示「有 N 处变动还没同步 · 点这里更新」（`checklistStale()` 双向对比缺失与多余） |
| 9 | 首页红区 | 新增「已过计划完成日」——以前只在项目页提示，首页看不到 |
| 10 | 项目「还缺这些」 | 新增「已返图但没记发布：发出去这套才算用完」 |
| 11 | 项目页右下角 FAB「＋」 | 按当前分页给最可能的新建：装备页→加装备（预置本项目+该角色+愿望单）、拍摄页→新建拍摄单、费用页→记一笔钱、概览页→加待办 |

---

## 三、更简洁（结构层面）

- **项目分页 5 → 4**：概览 / 装备 / **拍摄**（原「拍摄与人员」+「成片」合并）/ 费用。窄屏一排放得下，还能横向滑着翻。
- 首页顶部与装备页顶部各一条「怎么用滑动」提示（`swipeHintBox()`），点「知道了」永久关闭。
- 「我的 → **手势与手感**」两个开关：滑动快捷操作、滑动说明提示。**关掉滑动后行照样点开详情，电脑端完全不受影响**。
- 列表行去掉了一层只为滑动准备的包装：不可滑动的行退回普通 `.row` 渲染，DOM 更浅。

---

## 四、修复前 vs 修复后

| 指标 | 1.1.2 | 1.2 |
|---|---|---|
| 改一格状态的操作 | 点进行情 → 找 chip 或进表单 → 保存（3 步） | **一次滑动（1 步）+ 可撤销** |
| 手机上误触代价 | 改错要再点回去 | 4.6 秒内「撤销」一键回滚（含所有连带动作） |
| 项目分页横向宽度 | 5 个标签，窄屏挤压换行 | 4 个标签 + 横向滑动翻页 |
| 状态回退路径 | 只能进表单用「强制跳转」 | 装备 9 个状态可直接右滑退回 |
| 拍完到催片 | 手动新建交付物、手抄名字建待办 | 一键记原片交付（默认 7 天）、一键转待办 |
| 拍摄当天时间线 | 手抄各预约单时间 | 按预约单自动生成 |
| 清单是否过期 | 无提示 | 显示差 N 处 + 一键同步 |
| 首页漏看项 | 无 deadline 提醒 | 补「已过计划完成日」「已返图未记发布」 |
| 沙箱断言 | 61 条（57 通过，4 项设计如此） | 新增 78 条 **全通过**，回归集持平 |
| JS 报错 / 控制台输出 | 0 | **0** |

---

## 五、已知但有意不做的

1. **处置态不给滑动**：已售出 / 已报废 / 已归还 / 滑进外借中 一律走表单——去向 CN 是硬要求，滑动没法逼你填。
2. **取消 / 爽约 / 改期不给滑动**：三者都要填原因，同样留在表单里。
3. **鼠标不触发滑动**：电脑端仍是点击操作，避免选文字、拖动滚动条时误改状态。
4. **撤销窗口只有 4.6 秒，且之后有任何一次写入就失效**：不做操作历史栈，本地单人用不回滚长链条更安全。
5. **不改数据结构**：所以 1.1.2 与 1.2 可以随时来回切（共用同一存储键）。
6. 桌面端多列宽屏布局、缩略图、照片上传、小程序版 = 二期。

---

## 六、给你的操作建议

1. 直接双击 `cosplan-1.2.html`，数据自动接上（同一个存储键），无需导入导出。
2. 第一次先在「装备」页顶部看那条说明，拿示例数据随便滑两下，感受一下撤销按钮——**手滑不怕，4 秒内能撤**。
3. 手机上建议「添加到主屏幕」（Chrome / Edge 菜单里都有），全屏跑起来更像小程序。
4. 1.1.2 留着当回退点即可，日常用 1.2。
