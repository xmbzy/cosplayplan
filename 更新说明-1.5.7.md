# CosPlan v1.5.7 更新说明

文件：`cosplan-1.5.7.html` + **首个正式签名包 `release-kit/CosPlan-v1.5.7-release.apk`**
时间：2026-09-14
一句话：**拍摄单一键导出「装包单」实体文件（Word / Excel / CSV），并把发版从 debug 包换成正式签名 release 包（keystore 已生成，口令与清单写进 Word）。**

> 存储键 `cosplan.data.v1` 不变，无新字段、无迁移。
> **换签提醒**：手机上的 debug 包（1.5.4）与这个 release 包**签名不同**，必须先导出 JSON → 卸载 → 装 release → 导入。详见 `release-kit/CosPlan发布配置说明.doc` 第 4 节。

---

## 1. 这个版本只做一件事：出发前把配件找齐

入口：**拍摄单页 → 「导出装包单」**（就在「分享」旁边）。点开弹窗先给你三条数：**共 N 件 · 已装包 M 件 · 出发前还要处理 K 件**，然后三选一：

| 格式 | 出来是什么 | 什么时候选它 |
|---|---|---|
| **Word 打印版** `.doc` | A4 竖版，按装备大类分表，每行一个 **☐** 方框，抬头一段基本信息 | 打印出来贴在装箱内侧，出门前逐件打勾 |
| **Excel 表格** `.xls` | SpreadsheetML 一张表，8 列：勾选 / 名称 / 大类 / 分类 / 尺码 / **收纳位** / 数量 / 提醒 | 要在电脑上排序、筛"哪些还没填收纳位" |
| **CSV** `.csv` | 同上纯文本表，带 BOM | 前两种打不开时的兜底，任何表格软件都能开 |

**这张单子真正解决的是"为什么这件现在拿不到"**，「提醒」栏会把话说明白：

- `⚠ 已借给 阿坤，出发前要先要回来`（外借中）
- `未到手（在途）· 预计到货 2026-09-19`（愿望单/已下单/在途/修改中，有预期时间就带上）
- `已处置（报废/已售）· 拿不到`（别再翻箱倒柜）
- `易损：伞骨易断`、`仅剩 2（阈值 4）`
- **`未填收纳位`**（纸面显示「（没填）」）——找不齐配件八成的原因就是这个，去装备详情页「放在哪儿」填一下

实现上三条约束：

1. **一份算法三个出口**：`packPlanFor(sid)` 出数据，`packDocText / packXlsText / packCsvText` 只负责排版。不会出现"Word 里 9 件、Excel 里 8 件"。
2. **纯本机生成**：字符串 → `Blob` + `a.download`（与 JSON 备份同一条路）。新模块里 0 个 `fetch`/`XMLHttpRequest`/外链（构建时有门禁扫这一点）。
3. **纸面隐私**：只出姓名/角色/收纳位，**不出电话、不出网盘与作品集链接、不出场地详细地址**（只给场地名）。打印件会带去现场，掉了就是掉了。要联系毛娘妆娘，用通讯录的「点名字复制」或分享卡。

**顺手修的自己的设计缺陷**：第一版导出一次就把弹窗关了，想"Word 打印 + Excel 留档"必须重开一次。现在弹窗留着，可以连着点。

**中文不乱码**：三种格式都以 BOM 开头（CSV 是必需，Word/Excel 是防 Office 没有 BOM 时按本机 ANSI 猜编码）。

## 2. release APK：这次真正做了签名

| 项 | 结果（全部实测，不是配了就算） |
|---|---|
| keystore | `release-kit/keystore/cosplan-release.keystore`，RSA 2048、有效期 10000 天（到 2054-01-30）、别名 `cosplan`、证书 SHA1 `FC:33:F0:7D:…:5D` |
| 口令 | 22 位随机，**只写在两处**：`app/android/keystore.properties`（构建必需）与 `release-kit/CosPlan发布配置说明.doc`（你选的形式） |
| 构建 | `app/android/app/build.gradle` 加 `signingConfigs.release`（读 keystore.properties，**文件不在就自动退回不签名**，不阻塞别人构建）+ `versionCode 1507` / `versionName "1.5.7"` |
| 产物 | `app-release.apk` **3,836,334 字节**，MD5 `85a88bd6164f0eb7c6c79ee3c42a6142`；已复制为 `release-kit/CosPlan-v1.5.7-release.apk` |
| 验签 | `apksigner verify` → **Verifies**，v2 签名方案，`Signer #1 certificate DN: CN=CosPlan, OU=CosPlan, O=CosPlan, L=Shanghai, ST=Shanghai, C=CN`（原始输出留在 `release-kit/last-verify.txt`） |
| 包内实测 | `package name='com.cosplan.app' versionCode='1507' versionName='1.5.7'`、minSdk 24 / targetSdk 36；内嵌 `assets/public/index.html` = 386,832 字节，`ver:'1.5.7'` 与交付文件同尺寸 |
| `allowBackup` | 改成 **false**（你选的）：不让"设备备份"把 localStorage 与图片库带上云。代价写进了清单——换手机只能靠导出的 JSON |
| `INTERNET` | **保留没删**（评审说可删）。原因：Capacitor 的 WebView 靠本机 `127.0.0.1` 提供 `www/`，删了这个权限在部分 ROM 上会白屏——风险大于收益。应用自身不发任何请求 |
| WebView 调试 / 删权限 | 你这一轮没勾，未改 |

新增的发布工具（都在 `release-kit/`）：

- `CosPlan发布配置说明.doc`：Word/WPS 双击即开。含 **文件夹清单 / keystore 指纹 / 每次发版 7 步清单（每步都写成功标志）/ debug→release 必须先卸载的完整顺序 / release 与 debug 的实测差别 / 口令三份离线备份要求 / 报错对照表**。里面那行口令是现读自 `keystore.properties` 写进去的。
- `build-release-apk.bat`：一键重打（找 JDK → 查 keystore → 取最新 html → cap sync → assembleRelease → apksigner 验签，每步 `[OK]`/`[X]`）。纯 ASCII + CRLF（中文进 bat 会被 cmd 吃字节搞坏下一行）。
- 说明文档由 `node qa/mk157kit.js` 生成，**改过 keystore 或版本号后重跑一次**，文档不会过期。

## 3. 自检（`qa/run157_full.js`，14 套）

```
OK   94/0  _sandbox_test_157.js     1.3 基线沙箱
OK   77/0  qa/z157_k13_logic.js     K13 逻辑
OK   96/0  qa/z157_regress_96.js    96 条历史回归
OK   61/0  qa/z157_audit.js         口径一致性审计
OK    2/0  qa/g157_deadbtn.js       死按钮扫描
OK   29/0  qa/g157_features.js      1.5.5 特性守卫
OK    8/0  qa/g1575_fixes.js        1.5.5 评审修复守卫
OK   58/0  qa/g157_delta.js         1.5.6 定向守卫（本版当回归跑，一条没掉）
OK   31/0  qa/g157_pack.js          ★ 装包单：分组/提醒/计数/BOM/引号/公式注入/文件名/隐私/空态
OK    8/0  qa/g157_browser.js       真 Chromium：1.5.5 特性
OK   10/0  qa/g157_dl.js            ★ 真 Chromium：点按钮→真的落盘→读回来验（样品留在 qa-screenshots/导出样品/）
OK   19/0  qa/g157_ui.js            真 Chromium：1.5.6 那 9 件事仍成立
OK   10/0  qa/x157_swipe.js         真机逐点滑动 + 已归档真实渲染
OK    1/0  qa/x157_xss.js           19 个界面注入探测 0 逃逸
==== 合计 PASS 504 / FAIL 0 ====
```

构建侧的实测：`BUILD SUCCESSFUL`、`Verifies`、包内版本 1507/1.5.7、内嵌网页尺寸与交付文件一致（说明 `www/` 没同步旧版）。

## 4. 有意不做 / 留给下一步

1. **图片仍不进备份**：换机/换签装机后要重传图。html + 图片库打包 ZIP 是下一件事。
2. `advanceStatus` 与 `itemDisposalRules` 合并、费用锁搬独立表（从 1.3.4.x 挂到现在）。
3. 装包单**没有**做"导出 PDF"（浏览器打印即可，Word 版就是给打印用的）；也没有做"多选几场一起导"。
4. 装包单不含联系方式是刻意的，如果某场确实要把毛娘电话印上去，用分享卡或手写。
5. APK 侧未做：删除 `INTERNET`、关闭 release 的 WebView 远程调试、`targetSdk` 再升级、上架商店需要的 `mapping.txt`/`AAB`。

## 5. 需要你真机确认

1. **Word 版在手机上/电脑上打开是否正常**（尤其中文有没有乱码、表格宽度合不合适打印）；Excel 版能否正常筛选「收纳位」列为空的那些。样品在 `qa-screenshots/导出样品/`。
2. **release 包的首次安装流程**：按第 4 节顺序（导出 → 卸载 debug → 装 release → 导入），确认数据回来了、通知权限重新给过、`allowBackup=false` 后系统不再提示备份该应用。
3. **keystore 三份备份现在就做**（Word 第 6 节）：丢了密钥 = 现有安装永远收不到你的更新。
4. 装包单里你最想先看到的那一列是什么（现在顺序是 勾选/名称/大类/分类/尺码/收纳位/数量/提醒），要挪要减都可以。
